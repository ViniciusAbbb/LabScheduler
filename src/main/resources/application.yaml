spring:
  application:
    name: LabSchedule
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    url: jdbc:mysql://${DB_URL}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true